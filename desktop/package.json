{
  "name": "phone-farm-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Desktop wrapper for Phone Farm: one download, two first-run modes (set up a new host, or connect to an existing one).",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "test": "node --test test/*.test.js",
    "prepare:runtime": "node scripts/prepare-runtime.cjs",
    "dist:win": "npm run prepare:runtime && electron-builder --win",
    "dist:mac": "npm run prepare:runtime && electron-builder --mac"
  },
  "devDependencies": {
    "electron": "^33.0.0",
    "electron-builder": "^25.1.8"
  },
  "build": {
    "appId": "com.phonefarm.desktop",
    "productName": "Phone Farm",
    "files": [
      "**/*",
      "!dist/**"
    ],
    "extraResources": [
      {
        "from": "../system",
        "to": "system",
        "filter": [
          "**/*",
          "!storage/**",
          "!*.config.json",
          "!test-*.txt"
        ]
      }
    ],
    "win": {
      "target": "nsis"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true
    },
    "mac": {
      "target": "dmg",
      "category": "public.app-category.utilities"
    }
  }
}
