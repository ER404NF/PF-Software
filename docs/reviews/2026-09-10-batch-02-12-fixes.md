# Second batch of twelve — fixes and local verification

All twelve findings in [the review](2026-09-10-batch-02-12-findings.md) have fixes in the current working tree. Earlier changes are preserved.

| # | Fix | Regression evidence |
| --- | --- | --- |
| 1 | Device media has a dedicated devices namespace; reserved IDs and overlapping internal paths are rejected. | Reserved-name HTTP upload preserves session fixture; path isolation tests. |
| 2 | Model target text cannot expand action-specific control selectors. | open_profile with target Like selects the profile control rather than Like. |
| 3 | Pending selection blocks input; client actions carry their intended device ID; server rejects mismatches. | Browser-function fixture and stale-input WebSocket test. |
| 4 | Handoff timeout retains ERROR/unavailable mode and pending input; settlement permits recovery. | Timeout and later settlement test. |
| 5 | Emergency messages use a separate authenticated handler outside the normal input queue. | AI stop completes while another device's human tap is pending. |
| 6 | Task-scoped provider changes use complete task authorization. | Denied workspace grant prevents model-selection mutation. |
| 7 | Authorization, task ownership, and deadlines are rechecked after capture, before model submission. | Revocation and cancellation during capture result in zero provider calls. |
| 8 | Evidence capture rechecks task/lease state; interrupted runs use the actual outcome and do not append late candidates. | Cancellation during render leaves both task and run CANCELLED. |
| 9 | Index writes retain known identifiers and repoint related aliases to the current review. | ID-only and URL-only review updates propagate in both directions. |
| 10 | A nonempty incoming metrics snapshot replaces the previous snapshot. | New likes measurement survives read-back. |
| 11 | Scope dictionaries retain null prototypes during updates. | constructor, toString, and valueOf resolve correctly after updates and restart. |
| 12 | Authentication closure returns to login; network reconnect rechecks the session before retrying. | Client close-handler tests for 1008 and a subsequent HTTP 401. |

Full suite: **421/421 passed**, including **16 added tests**. Diff whitespace checks passed. Existing handoff tests were updated to expect an unavailable device on timeout.

Media compatibility: the configured mock device folders had no uploaded media to migrate. Other installations must relocate legacy per-device media to the new devices namespace; authentication and other internal folders must never be treated as media.

Limits: checks use local fixtures and source functions executed with DOM substitutes. Physical devices, live providers, and a rendered browser session remain unverified. Older clients may still send unscoped input; the current client supplies device identity and the server rejects mismatched identities.
