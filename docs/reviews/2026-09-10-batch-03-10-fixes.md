# Batch 3: fixes and validation

Implemented all ten findings from `2026-09-10-batch-03-10-findings.md`.

| # | Change | Regression coverage |
|---|---|---|
| 1 | Durable per-session revocation markers prevent late writes and touches from restoring logged-out authentication, including across store instances. | Delayed rename after logout, subsequent touch, and a fresh store remain unauthenticated. |
| 2 | Navigation selection excludes publishing controls and requires comments/post navigation labels. | View comments is selected instead of Post comment; a publishing-only screen is rejected. |
| 3 | Tap normalization uses the explicit viewport and rejects missing viewport or offscreen targets. | Oversized scroll content no longer changes the tap coordinates. |
| 4 | JSON tree traversal excludes hidden and disabled controls and their descendants. | Boolean and string visibility/enabled attributes and hidden ancestors are covered. |
| 5 | Verification returns NEEDS_HUMAN for a newly visible security challenge before any recovery. | A worker encounters a security code prompt after input and performs zero Home calls. |
| 6 | Authorization is checked after execution and before evidence writes, further observation, and screenshot fallback. | Revocation during capture produces zero saved evidence and zero subsequent observations; revoked tree failure produces no screenshot fallback. |
| 7 | Terminal research exits finalize an existing run centrally using the actual task outcome. | Cancellation between steps finalizes the existing candidate run as CANCELLED. |
| 8 | Stop releases AI authorization before persistence; a persistence failure holds the device in ERROR and prevents dispatch. | Injected ENOSPC leaves no active AI lease and no replacement task running. |
| 9 | Transient device errors retain client selection and Release while showing the error and clearing busy state. | Actual client message handler retains the owned device and Release. |
| 10 | Emergency stop retains the pending physical-action barrier until settlement. Human claims and AI dispatch respect that barrier. | Lease and WebSocket tests reject reuse before settlement and allow it afterward. |

Validation: `node --test "system/server/test/**/*.test.js"` passed **432/432**, with zero failures, skips, or cancellations. This adds 11 tests to the previous 421-test suite and updates existing ownership expectations. Focused tests passed **39/39**. `git diff --check` passed.

A failed cancellation write is reported as a persistence failure: the previous on-disk snapshot can still say RUNNING, while the live device is held in ERROR. This is not a successful durable cancellation. Session revocation markers are retained because old session IDs must never be reused.

Validation used local fixtures and client-function execution. Physical devices, live providers, and full browser interaction remain unverified. The original diagnostic probe asserts defective behavior and is retained as historical reproduction evidence; the server tests assert the corrected behavior. Existing unrelated workspace changes were preserved.
