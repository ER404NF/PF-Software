# Batch 12 fixes — local verification

All twelve findings in [the original report](2026-09-10-batch-12-findings.md) have implementation changes and regression coverage in the current working tree. Earlier uncommitted fixes were preserved.

| Finding | Change | Local validation |
| --- | --- | --- |
| 1. Wrong-device file actions | Capture device identity and request generation; discard stale lists; bind actions to their source device. | Controlled out-of-order responses and detached delete callback. |
| 2. Queue mutation authorization | Authorize task devices and research accounts before cancellation, priority changes, and reordering, including the reorder target. | Restricted-admin HTTP requests are denied without mutation. |
| 3. Research device permission revocation | Live device and account checks at scheduler dispatch and worker authorization boundaries. | Revocation during planning and persisted admission grants after restart. |
| 4. AI cancellation overlaps input | Revoke input immediately; retain the lease until pending input settles. Stop and expiry use the same release path. | Pending-action tests for cancellation, stop, window expiry, and duration expiry. |
| 5. Cropped screenshot tap geometry | Preserve image aspect ratio and map taps against the image element bounds. | Actual click-handler execution with portrait and landscape geometry; CSS assertions. |
| 6. Screenshot fallback | Permit passive screenshot observations; ungrounded navigation requests human handoff without Home recovery. | Real worker and Instagram skill with screenshot-only observations. |
| 7. Rotation coordinates | Read current WDA dimensions for each coordinate transformation. | Loopback WDA fixture rotates dimensions between taps and a positioned swipe. |
| 8. Screenshot capture no-op | Acquire an image, save durable evidence, verify its reference, and checkpoint it. | Read back saved artifact bytes with a UI tree available. |
| 9. Duration ignored | Validate positive finite limits; enforce elapsed time per attempt, including pauses; check deadlines before worker execution. | Scheduler expiry, paused expiry, invalid values, and a late model decision. |
| 10. Torn audit tail | Add a missing record boundary on log opening while preserving the torn tail. | First post-restart event remains readable. |
| 11. Historical active-task display | Include only RUNNING and PAUSED tasks in the active-device map. | Actual client function with running, cancelled, and queued entries. |
| 12. HTTPS WebSocket URL | Select wss for HTTPS and ws for HTTP, retaining host and port. | Actual URL construction for both protocols. |

Validation: full Node suite **405/405 passed**; `git diff --check` passed. New coverage adds 21 tests. Existing tests were updated where they assumed cached dimensions, anonymous fixture creators, or immediate release of pending AI work.

Limits: browser checks use Node VM/DOM fixtures and geometry assertions, not a rendered browser session. Physical devices, live model providers, and a deployed HTTPS proxy remain unverified. A device with an action that never settles remains reserved rather than allowing overlapping input.
